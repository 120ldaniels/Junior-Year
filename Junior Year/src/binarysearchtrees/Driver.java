package binarysearchtrees;

public class Driver {
	
	public static void main(String[] args) {
	BinarySearchTree bst = new BinarySearchTree();
	
	bst.insert(30);
    bst.insert(20); 
    bst.insert(40); 
    bst.insert(70); 
    bst.insert(60); 
    bst.insert(80); 
    
    bst.printInorder();
	
	// height();
	 
	 
	 
	 /*
	 insert(double d); //there should be no duplicate data in the tree
	 search(double d);
	 delete(double d);
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 
	 BinarySearchTree convert(BinaryTree<Double> bt);
	 BinarySearchTree convert(LinkedList<Double> ll); //precondition: ll must be sorted
	 BinarySearchTree convert(double[] arr); //precondition arr must be sorted
*/
	
	
	}
}
